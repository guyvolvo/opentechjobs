sql.js-httpvfs 0.8.12, vendored verbatim from the npm dist. Three files, unmodified: the UMD entry, the worker, and the wasm the worker loads from beside itself. Pinned because the site has no bundler and no build step, so this is the dependency.
